plugins {
    id 'application'
}

repositories {
    mavenLocal()
    mavenCentral()
}

tasks.register('print') {
    doLast {
        //gradle print -PmyPropName="Text"
        println project.hasProperty('myPropN1ame')
        println project.hasProperty('myPropName')
    }
}